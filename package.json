{
  "name": "task-queue",
  "version": "1.0.0",
  "description": "Typed local task queue with concurrency limit, dedup, retries and graceful shutdown",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
